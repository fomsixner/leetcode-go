package leetcode_go

/*
 * @lc app=leetcode.cn id=877 lang=golang
 *
 * [877] 石子游戏
 */

// @lc code=start
// 直接return true
func stoneGame(piles []int) bool {
	// count, sign := 0, 1
	// left, right := 0, len(piles)-1
	// for left < right {
	// 	if piles[left] > piles[right] {
	// 		count += piles[left] * sign
	// 		left++
	// 	} else {
	// 		count += piles[right] * sign
	// 		right--
	// 	}
	// 	sign *= -1
	// }
	// return count > 0
	return true
}

// @lc code=end
